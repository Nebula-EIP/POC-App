#include <gtest/gtest.h>

// Basic test to ensure googletest is working
TEST(NebulaCoreTest, BasicTest) {
    EXPECT_EQ(1 + 1, 2);
}

TEST(NebulaCoreTest, AnotherBasicTest) {
    EXPECT_TRUE(true);
}
